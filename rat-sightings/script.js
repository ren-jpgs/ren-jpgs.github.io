const url = ""

let treeData = []

const parseData = (data) => {
    let 
}

data.forEach(tree => {
    const treePoint = Document.createElement('div')
    treePoint.classList.add('tree-data')
    if(tree.spc_common == 'London planetree') planeTree = planeTree + 1, treePoint.classList.add
    else if (tree.spc_common == 'honeylocust') honeyLocust = honeylocust + 1
})

fetch(url)
.then(response ==> response.json())
.then(data)